
module.exports = {
    WebServer: require('./src/WebServer'),

    WebRequest: require('./src/WebRequest'),
    WebResponse: require('./src/WebResponse'),

    /* trilliant "Service" requirement */
    TrilliantService: require('./src/TrilliantService')
};
